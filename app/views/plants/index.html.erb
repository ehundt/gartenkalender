<h3>Meine Pflanzen</h3>
<table class="table plant-list">

<% @plants.each do |plant| %>
  <tr>
    <td><% if plant.main_image.file? %><%= link_to image_tag(plant.main_image.url(:thumb)), plant %><% end %></td>
    <td><%= link_to plant.name, plant %></td>
    <td><%= plant.subtitle %></td>
    <td class="active-button">
      <% if plant.active %>
        <%= image_tag "plant_active.gif", height: '42px', width: '30px' %>
      <% else %>
        <%= image_tag "plant_inactive.gif", height: '42px', width: '30px' %>
      <% end %>
    </td>
    <td>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        <%= link_to 'Ändern', edit_plant_path(plant) %>
    </td>
    <td>
      <span class="glyphicon glyphicon-remove-circle" aria-hidden="true"></span>
      <%= link_to 'Entfernen', plant, method: :delete, data: { confirm: 'Sind Sie sicher?' } %>
    </td>
  </tr>
<% end %>
</table>
