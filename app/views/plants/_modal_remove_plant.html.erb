<div class="modal fade" id="removePlantModal<%= plant.id %>" tabindex="-1" role="dialog" aria-labelledby="removePlantModalLabel<%= plant.id %>" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-body">
        <div class="row modal-remove-plant">
          <% if plant.main_image.file? %>
            <div class="col-xs-4">
              <%= image_tag(download_main_image_plant_path(plant, {size: :thumb}), class: "img-rounded img-responsive") %>
            </div>
            <div class="col-xs-8">
          <% else %>
            <div class="col-xs-12">
          <% end %>
            <p>Möchtest Du Deine Pflanze "<%= plant.name %>" unwiderruflich löschen?</p>
          </div>
        </div>
        <div class="row">
          <div class="col-xs-12">
            <p>Durch Klicken auf <%= image_tag("plant_active.gif") %> kannst Du sie auch nur inaktivieren.</p>
          </div>
        </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Abbrechen</button>
          <%= link_to 'Löschen', plant, method: :delete, class: "btn btn-default", type: "button" %>
        </div>
      </div>
    </div>
  </div>
</div>
