<% content_for :sidebar do %>
  <% if current_user && @plant %>
    <div class="row sidebar-plant">
      <div class="col-xs-12">
        <%= link_to image_tag(download_main_image_plant_path(@plant, {size: :medium}), class: "sidebar-image img-rounded img-responsive"), @plant  %>
      </div>
    </div>
    <% if @plant.user == current_user && @plant.creator != current_user %>
      <div class="row sidebar-plant">
        <div class="col-xs-12">
          <% if @plant.active %>
            <%= image_tag "plant_active.gif", height: '42px', width: '30px' %> aktiv
          <% else %>
            <%= image_tag "plant_inactive.gif", height: '42px', width: '30px' %> nicht aktiv
          <% end %>
        </div>
      </div>
    <% end %>

    <% if @plant.creator != current_user %>
      <div class="row sidebar-plant">
        <div class="col-xs-12">
          <span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
          <br>
          <% if current_user.voted_for?(@plant.original) %>
            als besonders hilfreich markiert
          <%# TODO: hier könnte ein tooltip hin: von mir als besonders hilfreich markiert %>
          <%# TODO: undo voting %>
          <% else %>
            <%= link_to 'Original als besonders hilfreich markieren', vote_plant_path(@plant.original), method: :post %>
          <% end %>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>
