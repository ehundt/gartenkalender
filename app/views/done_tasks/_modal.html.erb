<%= form_for :done_task, method: :post, url: plant_task_done_tasks_path(task.plant, task) do |f| %>
  <input type="hidden" name="done_task[skipped]" id="done_task_skipped" value="0" />
  <div class="modal fade" id="taskDoneModal<%= task.id %>" tabindex="-1" role="dialog" aria-labelledby="taskDoneModalLabel<%= task.id %>" aria-hidden="true">
    <div class="modal-dialog modal-sm">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="taskDoneModalLabel<%= task.id %>">Aufgabe erledigt</h4>
        </div>
        <div class="modal-body">
          <h4><%= task.plant.name %>: <%= task.title %></h4>
          <p>Persönliche Notiz (optional):</p>
            <%= f.text_area :notice, :class => "form-control", rows: "4" %>
          <br>
          <p>erledigt am:</p>
            <%= date_select :done_task, :date %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Abbrechen</button>
          <button type="submit" class="btn btn-success">Erledigt</button>
        </div>
      </form>
      </div>
    </div>
  </div>
  <% end %>



