<div class="new-share-desc">
  Willst Du Deine Pflanzen inklusive Aufgaben mit jemandem teilen?
  Dann gebe hier seine/ihre Email Adresse ein.
  Wenn diese(r) GärtnerIn zugestimmt hat, könnt ihr anfangen zu teilen.
</div>

<%= form_tag(action: "create") do |f| %>
  <%= email_field(:user, :email) %>
  <%= submit_tag "anfragen" %>
<% end %>
<br />
<% unless @sharing_connections.empty? %>
  <h4>GärtnerInnen, mit denen ich bereits Pflanzen teile</h4>
  <table class="table plant-list">
    <% @sharing_connections.each do |connection| %>
      <tr>
        <td>
          <%= connection.sharing_user_for(current_user).display_name %>
        </td>
      </tr>
    <% end %>
  </table>
<% end %>

<% unless @requesting_connections.empty? %>
  <h4>Anfragen an mich</h4>
  <table class="table plant-list">
  <% @requesting_connections.each do |connection| %>
    <tr>
      <td><%= connection.requesting_user.display_name %></td>
      <td>bestätigen?
      </td>
      <td>
        <!-- Single button -->
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#"><%= link_to "bestätigen", user_connection_path(connection, confirmed: true), method: :patch %></a></li>
            <li><a href="#"><%= link_to "ablehnen", user_connection_path(connection, confirmed: false), method: :patch %></a></li>
          </ul>
        </div>
      </td>
    </tr>
  <% end %>
  </table>
<% end %>

<% unless @requested_connections.empty? %>
  <h4>Meine noch nicht bestätigten Anfragen</h4>
  <table class="table plant-list">
  <% @requested_connections.each do |connection| %>
    <tr>
      <td><%= connection.requested_user.display_name %></td>
    </tr>
  <% end %>
  </table>
<% end %>
