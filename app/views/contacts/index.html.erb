<h4>Befreundete GärtnerInnen kontaktieren</h4>
<p>
  Willst Du Deine Pflanzen inklusive Aufgaben mit jemandem teilen?
  Dann gebe hier seine/ihre Email Adresse ein.
  Wenn diese(r) GärtnerIn zugestimmt hat, könnt ihr anfangen zu teilen.
</p>

<%= form_tag(action: "create") do |f| %>
  <%= email_field(:user, :email) %>
  <%= submit_tag "anfragen" %>
<% end %>
<br />

<% unless @sharing_contacts.empty? %>
  <h4>GärtnerInnen, mit denen ich bereits Pflanzen teile</h4>
  <table class="table">
    <% @sharing_contacts.each do |contact| %>
      <% user = contact.sharing_user_for(current_user) %>
      <tr>
        <td>
          <% if user.picture.file? %><%= link_to image_tag(user.picture.url(:thumb)), user %><% end %>
        </td>
        <td>
          <%= user.display_name %>
        </td>
      </tr>
    <% end %>
  </table>
<% end %>

<% unless @requesting_contacts.empty? %>
  <h4>Anfragen an mich</h4>
  <table class="table">
  <% @requesting_contacts.each do |contact| %>
    <tr>
      <td><%= contact.requesting_user.display_name %></td>
      <td>bestätigen?
      </td>
      <td>
        <!-- Single button -->
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#"><%= link_to "bestätigen", contact_path(contact, confirmed: true), method: :patch %></a></li>
            <li><a href="#"><%= link_to "ablehnen", contact_path(contact, confirmed: false), method: :patch %></a></li>
          </ul>
        </div>
      </td>
    </tr>
  <% end %>
  </table>
<% end %>

<% unless @requested_contacts.empty? %>
  <h4>Meine noch nicht bestätigten Anfragen</h4>
  <table class="table">
  <% @requested_contacts.each do |contact| %>
    <tr>
      <td><%= contact.requested_user.display_name %></td>
    </tr>
  <% end %>
  </table>
<% end %>
